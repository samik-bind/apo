---
title: Quickstart
description: Set up apo and run your first Task — publish, connect, and see results.
---

import TerminalDemo from '../../components/demos/TerminalDemo.astro';

**Zero to a real result, as fast as possible.** The workflow is: publish your Tasks, connect your machine, run from the dashboard or CLI, and read the breakdown.

## 1. Bring up the stack

Self-host from source:

```bash
git clone <repo-url> && cd apo
docker compose up -d --build
```

Wait for readiness:

```bash
curl -fsS http://localhost:8000/health/ready | jq
# {"ok": true, ...}
```

Open `http://localhost:3000` and create the first admin account. Full details in [Self-Hosting](/self-hosting/topology/).

## 2. Authenticate

<TerminalDemo
  command="apo login"
  lines={[
    { parts: [{ text: "Email: you@example.com" }] },
    { parts: [{ text: "Password: ********" }] },
    { parts: [{ text: "✓ Logged in as you@example.com", tone: "success" }] },
    { text: "  Backend:  http://localhost:8000" },
    { text: "  Project:  acme-eval (a1b2c3d4)" },
  ]}
/>

## 3. Publish your Task catalog

Tasks live in your code — authored in TypeScript using the [`@apo/sdk`](https://www.npmjs.com/package/@apo/sdk) package. Install it in your application's repository first:

```bash
pnpm add @apo/sdk
```

See [Define a Task](/guides/define-a-task/) for the authoring flow. Once defined, publish their metadata so the dashboard and `apo connect` can see them:

<TerminalDemo
  command="apo task publish --dir ./my-tasks"
  lines={[
    { parts: [{ text: "✓ Published 2 tasks to acme-eval", tone: "success" }] },
    { text: "  digest: sha256:a1b2c3d4..." },
    { text: "  published_at: 2026-07-31T12:00:00Z" },
  ]}
/>

This sends only Task IDs, display names, adapter names, and check flags — no source code, prompts, or credentials.

## 4. Run a Task from the CLI

For a one-shot run in your local workspace:

<TerminalDemo
  command="apo task run extract-parties"
  lines={[
    { parts: [{ text: "Running extract-parties...", tone: "dim" }] },
    { text: "" },
    { parts: [{ text: "PASS", tone: "success" }, { text: " extract-parties", tone: "bold" }] },
    { parts: [{ text: "  Checks:", tone: "bold" }] },
    { parts: [{ text: "    " }, { text: "PASS", tone: "success" }, { text: " used-source-document" }] },
    { parts: [{ text: "    " }, { text: "PASS", tone: "success" }, { text: " parties-are-complete" }] },
  ]}
/>

The Task runs locally against your current source. The result, trace, and checks are recorded to the server.

## 5. Connect for dashboard runs

For persistent execution — so the dashboard and Schedules can dispatch work to your machine:

<TerminalDemo
  command="apo connect --dir ./my-tasks"
  lines={[
    { text: "Discovered 2 tasks in ./my-tasks" },
    { parts: [{ text: "Connected to acme-eval · catalog matches · capacity 4", tone: "success" }] },
    { text: "Waiting for assignments…  Ctrl+C to stop" },
  ]}
/>

Now any Run created from the dashboard Tasks page or a Schedule is claimed and executed on this machine. The Run's Trace, Tests, and Deliverables appear in the dashboard.

## 6. Run from the dashboard

Open `http://localhost:3000`, go to **Tasks**, select one or more Tasks, and click **Run**. The Run is queued, claimed by your connected machine, and executed locally.

The result is binary — pass or fail. Click any Run to see the full breakdown: which tests passed, which failed, and the trace showing every call the agent made.

## The workflow

```text
apo task publish     → publish your Task metadata (no source)
apo connect          → connect as a persistent executor
dashboard / schedule → queue work to your machine
                     → Tasks execute against your local source
                     → results, traces, and deliverables flow back
```

At no point does the server receive your source code, repository credentials, or environment variables. The Control Plane coordinates identity and metadata; your machine owns execution.

## Next

- [apo connect](/cli/connect/) — the persistent executor
- [apo task publish](/cli/task-publish/) — catalog publication
- [apo task run](/cli/task-run/) — one-shot CLI execution
- [Tasks](/concepts/tasks/) — how Tasks are structured
- [Tests](/concepts/tests/) — code assertions and LLM judges
