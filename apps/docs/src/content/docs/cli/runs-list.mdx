---
title: "apo runs list"
description: "List past runs from the backend, filtered by task or status."
---

import TerminalDemo from '../../../components/demos/TerminalDemo.astro';
import TerminalOutput from '../../../components/demos/TerminalOutput.astro';

List task runs from the backend. Filter by task, status, model, or effort.

<TerminalDemo
  command="apo runs list"
  lines={[
    { text: "Run ID    Task             Batch    Status     Result   Execution        Cost      Started" },
    { text: "────────  ───────────────  ───────  ─────────  ───────  ───────────────  ────────  ─────────────────────" },
    { text: "de89cab0  extract-parties  -        completed  FAIL     gpt-5 · high     $0.0034   7/4/2026, 5:12 PM" },
    { text: "c3d4e5f6  code-review      b9c0d1   completed  PASS     claude-haiku · -  $0.0091   7/4/2026, 4:03 PM" },
    { text: "a7b8c9d0  extract-parties  -        completed  FAIL     gpt-5 · high     $0.0028   7/4/2026, 2:45 PM" },
    { text: "" },
    { text: "3 runs", tone: "default" },
  ]}
/>

## Synopsis

```bash
apo runs list [--task <id>] [--status <s>] [--model <m>] [--effort <e>] [--limit <n>]
```

## Options

| Option | Default | Description |
|---|---|---|
| `--task <id>` | all | Only runs of this task. |
| `--status <s>` | all | Filter by status (e.g. `completed`, `running`). |
| `--model <m>` | all | Filter by adapter-reported model (repeatable; OR within model). |
| `--effort <e>` | all | Filter by adapter-reported effort (repeatable; OR within effort). |
| `--limit <n>` | backend default | Maximum runs to list. |

## Output

<TerminalOutput
  lines={[
    { text: "Run ID    Task             Batch    Status     Result   Execution        Cost      Started" },
    { text: "────────  ───────────────  ───────  ─────────  ───────  ───────────────  ────────  ─────────────────────" },
    { text: "de89cab0  extract-parties  -        completed  FAIL     gpt-5 · high     $0.0034   7/4/2026, 5:12 PM", tone: "danger" },
    { text: "c3d4e5f6  code-review      b9c0d1   completed  PASS     claude-haiku · -  $0.0091   7/4/2026, 4:03 PM", tone: "success" },
    { text: "a7b8c9d0  extract-parties  -        completed  FAIL     gpt-5 · high     $0.0028   7/4/2026, 2:45 PM", tone: "danger" },
    { text: "" },
    { text: "3 runs", tone: "default" },
  ]}
/>

`RESULT` is `PASS` or `FAIL` for completed runs, `—` while running. The `Execution` column shows the adapter-reported model and effort. Pass `--json` for machine-readable output.

## See also

- [`apo runs show`](/cli/runs-show/) — drill into one run's checks and failures.
- [`apo task run`](/cli/task-run/) — produce a new run.
- [CLI overview](/cli/) — the full command surface.
