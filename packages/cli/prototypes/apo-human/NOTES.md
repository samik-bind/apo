# NOTES — verdict placeholder

Fill in after driving the prototype, then delete the prototype.

- [ ] Which variant felt right? (wizard / menu / dashboard / mix — which parts?)
- [ ] Did the env preflight + cheatsheet remove the "which env vars does apo
      need" guessing, or is it noise?
- [ ] Should a real build be a separate command (`apo run`, `apo go`) or an
      interactive fallback when `task run` gets no args on a TTY?
- [ ] What's missing that the dashboard UI still does better?
- [ ] Decision: absorb as … / drop.
